Unfortunately, I can't upload the .lib files, so you have to build them yourself.
ImageMagick has a great tutorial to do that.

Here's the link: https://github.com/ImageMagick/ImageMagick-Windows
NOTE: You have to build it as a static lib.
